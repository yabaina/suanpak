{
  "name": "suanpak-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "cd ../frontend && npm install && npm run build",
    "start": "node index.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
